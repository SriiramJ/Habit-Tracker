export enum HabitStatus{
    ACTIVE = "active",
    COMPLETED= "completed",
    ARCHIVED = "aarchived"
}

// Notification types
export enum NotificationType{
    PUSH = "push",
    EMAIL = "email",
    REMINDER = "reminder",
}

// Reward types
export enum RewardType {
  BADGE = "badge",
  POINTS = "points",
  TROPHY = "trophy",
}

// Other static values
export const STREAK_ICON = "🔥";
export const APP_NAME = "Habit Tracker AI";